# Issue #6: 实现VLM服务模块

## 📋 问题描述

文档中提到的视觉大模型服务模块（`easymocap/vlm/service.py`）在源码中尚未实现，需要完成开发。

## 🎯 目标

- [ ] 集成SAM分割模型
- [ ] 集成Grounding DINO检测模型
- [ ] 实现人物分割功能
- [ ] 实现关键点增强功能

## 📝 具体修改

### 1. 创建目录结构

```
easymocap/vlm/
├── __init__.py
├── service.py      # 主服务模块
├── sam.py          # SAM分割
├── grounding.py    # Grounding DINO检测
└── enhance.py      # 关键点增强
```

### 2. 核心功能

#### service.py - VLM服务

```python
class VLMService:
    """视觉大模型服务"""
    
    def segment_person(self, image: np.ndarray, bbox: List[float]) -> np.ndarray:
        """使用SAM精确分割人物"""
        # 调用SAM模型
        # 返回分割mask
        pass
    
    def detect_with_grounding(self, image: np.ndarray, text_prompt: str) -> List[Detection]:
        """使用Grounding DINO进行开放词汇检测"""
        # 调用Grounding DINO
        # 返回检测结果
        pass
    
    def enhance_keypoints(self, image: np.ndarray, keypoints: np.ndarray, 
                         confidence: np.ndarray) -> Tuple[np.ndarray, np.ndarray]:
        """使用VLM增强关键点检测"""
        # 识别低置信度关键点
        # 使用视觉大模型修复
        # 返回增强后的关键点
        pass
```

#### sam.py - SAM分割

```python
class SAMSegmentor:
    """SAM分割器"""
    
    def __init__(self, model_path: str, model_type: str = "vit_h"):
        # 加载SAM模型
        pass
    
    def segment(self, image: np.ndarray, bbox: np.ndarray) -> np.ndarray:
        """执行分割"""
        # 预处理图像
        # 执行分割
        # 后处理mask
        pass
```

### 3. 模型文件管理

创建 `models/` 目录结构：

```
models/
├── sam/
│   ├── sam_vit_h.pth
│   └── sam_vit_l.pth
├── grounding_dino/
│   ├── config.py
│   └── checkpoint.pth
└── pretrained/
    ├── hrnet.pth
    └── spin.pth
```

### 4. 集成到现有系统

```python
# easymocap/pipeline/basic.py
from ..vlm import VLMService

def multi_stage_optimize(body_model, params, kp3ds, ...):
    # 使用VLM增强关键点
    vlm = VLMService()
    kp3ds_enhanced, confidence = vlm.enhance_keypoints(
        image, kp3ds, confidence
    )
    
    # 使用增强后的关键点进行优化
    # ...
```

## 🏷️ 标签

- `priority: high`
- `type: feature`
- `component: vlm`
- `component: ai`

## 📊 预估工作量

- 时间：1周
- 复杂度：高

## ✅ 验收标准

1. SAM分割功能可用
2. Grounding DINO检测功能可用
3. 关键点增强功能可用
4. 模型文件管理规范
5. 单元测试通过
6. 性能测试通过

## 📚 参考资料

- [SAM论文](https://arxiv.org/abs/2304.02643)
- [Grounding DINO论文](https://arxiv.org/abs/2303.05499)
- [Segment Anything GitHub](https://github.com/facebookresearch/segment-anything)
- [Grounding DINO GitHub](https://github.com/IDEA-Research/GroundingDINO)
