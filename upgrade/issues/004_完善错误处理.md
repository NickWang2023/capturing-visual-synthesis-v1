# Issue #4: 完善错误处理

## 📋 问题描述

当前代码的错误处理不完善，存在以下问题：
- 使用裸 `except:` 捕获异常
- 缺少日志记录
- 错误信息不明确

## 🎯 目标

- [ ] 替换裸 `except:` 为具体异常类型
- [ ] 添加日志记录
- [ ] 改进错误信息

## 📝 具体修改

### 1. neuralbody/model/neuralbody.py

```python
# 修改前
try:
    if spconv.__version__.split('.')[0] == '2':
        import spconv.pytorch as spconv
except:
    pass

# 修改后
try:
    if spconv.__version__.split('.')[0] == '2':
        import spconv.pytorch as spconv
except ImportError:
    logger.warning("spconv v2 未安装，使用默认版本")
except Exception as e:
    logger.error(f"导入spconv失败: {e}")
```

### 2. pipeline/basic.py

```python
# 修改前
def multi_stage_optimize(body_model, params, kp3ds, ...):
    # 无错误处理

# 修改后
def multi_stage_optimize(body_model, params, kp3ds, ...):
    try:
        # 优化逻辑
        # ...
    except ValueError as e:
        logger.error(f"输入数据格式错误: {e}")
        raise
    except RuntimeError as e:
        logger.error(f"优化过程失败: {e}")
        raise
    except Exception as e:
        logger.error(f"未知错误: {e}")
        raise RuntimeError(f"优化失败: {e}") from e
```

### 3. 添加日志配置

创建 `easymocap/logging_config.py`：

```python
import logging
import sys
from typing import Optional

def setup_logging(
    level: int = logging.INFO,
    log_file: Optional[str] = None,
    log_format: str = '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
) -> None:
    """配置日志系统"""
    # 创建格式器
    formatter = logging.Formatter(log_format)
    
    # 控制台处理器
    console_handler = logging.StreamHandler(sys.stdout)
    console_handler.setFormatter(formatter)
    
    # 文件处理器（可选）
    handlers = [console_handler]
    if log_file:
        file_handler = logging.FileHandler(log_file, encoding='utf-8')
        file_handler.setFormatter(formatter)
        handlers.append(file_handler)
    
    # 配置根日志器
    logging.basicConfig(
        level=level,
        handlers=handlers,
        force=True
    )
```

## 🏷️ 标签

- `priority: high`
- `type: enhancement`
- `component: code-quality`
- `component: error-handling`

## 📊 预估工作量

- 时间：2天
- 复杂度：中

## ✅ 验收标准

1. 所有裸 `except:` 已替换
2. 关键函数添加日志记录
3. 错误信息清晰明确
4. 日志输出格式统一

## 📚 参考资料

- [Python日志指南](https://docs.python.org/3/library/logging.html)
- [Python异常处理最佳实践](https://docs.python.org/3/tutorial/errors.html)
